from django import forms
from .models import Posts

# class PostForm(forms.ModelForm):
    # class Meta:
        # model = Posts
        # fields = ['post_text', 'image']
        # widgets = {
            # 'post_text': forms.TextInput(attrs={'placeholder': "What's happening?"}),
        # }
